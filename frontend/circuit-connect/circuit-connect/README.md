# ⚡ Circuit Connect — 회로 연결 퍼즐 게임

> 토스 앱인토스(Apps in Toss) 미니앱으로 출시하는 회로 퍼즐 게임 + 실시간 데이터 파이프라인 프로젝트

## 📋 프로젝트 개요

**Circuit Connect**는 회로 조각을 회전시켜 전원에서 전구까지 전기를 연결하는 퍼즐 게임입니다.  
게임 자체뿐 아니라, 유저 행동 로그를 실시간으로 수집·처리·분석하는 **데이터 엔지니어링 파이프라인**을 함께 구축하여, 게임 운영과 데이터 기반 의사결정을 실현하는 것이 핵심 목표입니다.

| 목적 | 설명 |
|------|------|
| 🎮 게임 서비스 | 앱인토스에 미니앱으로 출시, 실제 유저 확보 |
| 📊 데이터 파이프라인 | 유저 행동 로그 실시간 수집 → 처리 → 적재 → 시각화 |
| 📁 포트폴리오 | 데이터 엔지니어링 역량을 실서비스 기반으로 증명 |

---

## 🏗️ 시스템 아키텍처

```
┌──────────────────────────────────────────────────────────┐
│                    프론트엔드                               │
│                                                          │
│   Next.js + TypeScript                                    │
│   → 게임 UI, 이벤트 로깅, 랭킹 화면                          │
│                                                          │
└────────────────────────┬─────────────────────────────────┘
                         │ HTTPS (이벤트 로그 전송)
                         │ via Caddy Reverse Proxy
                         ▼
┌──────────────────────────────────────────────────────────┐
│           미니 PC 서버 (On-Premise)                        │
│           Intel N100 · 16GB RAM · Ubuntu 24.04            │
│                                                          │
│   ┌─────────────┐    ┌─────────────┐                     │
│   │  FastAPI     │───▶│   Kafka     │                     │
│   │  (이벤트API) │    │ (circuit-   │                     │
│   │  (랭킹 API) │    │  connect-   │                     │
│   │  (유저 API)  │    │  events)    │                     │
│   └─────────────┘    └──────┬──────┘                     │
│         │                   │                             │
│         ▼                   ▼                             │
│   ┌──────────┐    ┌──────────────────┐                   │
│   │PostgreSQL│    │   ClickHouse     │                   │
│   │(운영 DB) │    │  Kafka Engine    │                   │
│   │- users   │    │  + Materialized  │                   │
│   │- rankings│    │    View          │                   │
│   │- progress│    │  (자동 적재)      │                   │
│   └──────────┘    └────────┬─────────┘                   │
│                            │                              │
│              ┌─────────────┼─────────────┐               │
│              ▼             ▼             ▼               │
│        ┌──────────┐ ┌──────────┐ ┌──────────┐          │
│        │ Grafana  │ │   n8n    │ │  Google  │          │
│        │(9패널    │ │(자동화)  │→│  Sheets  │          │
│        │ 대시보드) │ │ 일일 로그│ │ (백업)   │          │
│        └──────────┘ └──────────┘ └──────────┘          │
└──────────────────────────────────────────────────────────┘
```

### 인프라 접근 경로
```
인터넷 → 가비아 DNS (calmee.store) → Caddy Reverse Proxy → 내부 서비스
  ├── api.calmee.store     → FastAPI (:8000)
  ├── grafana.calmee.store → Grafana (:3000)
  └── n8n.calmee.store     → n8n     (:5678)
```

---

## 🎮 게임 컨셉

### 핵심 메카닉
- **그리드 기반 퍼즐**: N×M 그리드 위에 회로 조각(직선, L자, T자, +자)이 배치됨
- **탭하여 회전**: 회로 조각을 탭하면 90도씩 회전
- **경로 연결**: 전원(⚡) → 전구(💡)까지 전기가 흐르는 경로를 완성하면 클리어
- **네온 점등 연출**: 클리어 시 연결된 경로를 따라 네온 라이트가 순차적으로 켜지는 애니메이션

### 게임 모드

#### 🔹 스토리 모드
- 사전 설계된 스테이지 (5개 챕터 × 10스테이지)
- 난이도 점진적 상승: 3×3 → 4×4 → 5×5
- 보너스 경유 포인트 포함

#### 🔹 타임어택 모드
- 제한 시간(60/120/180초) 내 최대한 많은 스테이지 클리어
- 빠를수록 높은 점수
- 랭킹/리더보드와 연동

---

## 🛠️ 기술 스택

### 프론트엔드
| 기술 | 용도 |
|------|------|
| Next.js + TypeScript | 게임 UI 및 로직 |
| CSS Animation + Glow Effects | 네온 이펙트 |

### 백엔드 API
| 기술 | 용도 |
|------|------|
| FastAPI (Python) | 이벤트 수집 + 랭킹 + 유저 API |
| aiokafka | 비동기 Kafka Producer |
| asyncpg | 비동기 PostgreSQL 클라이언트 |
| orjson | 고성능 JSON 직렬화 |

### 데이터 파이프라인
| 기술 | 용도 |
|------|------|
| Apache Kafka | 이벤트 로그 메시지 큐 |
| ClickHouse Kafka Engine | Kafka → ClickHouse 자동 적재 |
| ClickHouse Materialized View | 실시간 데이터 변환 |
| PostgreSQL | 유저/랭킹/진행도 운영 DB |
| Grafana | 실시간 대시보드 시각화 (9패널) |
| n8n | ClickHouse → Google Sheets 일일 자동 백업 |

### 인프라
| 기술 | 용도 |
|------|------|
| 미니 PC (Intel N100, 16GB) | 24/7 On-Premise 서버 |
| Docker Compose | 전체 파이프라인 컨테이너 관리 |
| Caddy | 리버스 프록시 + 자동 HTTPS |
| 가비아 DNS (calmee.store) | 도메인 관리 |

---

## 📊 Grafana 대시보드

> 🔗 Live: `https://grafana.calmee.store`

### 9패널 구성

**Row 1 — KPI (5패널)**

| 패널 | 설명 | 색상 |
|------|------|------|
| Total Events | 전체 이벤트 수 | 쿨 화이트 |
| Active Users 24h | 24시간 활성 유저 | 쿨 화이트 |
| Completion Rate | 게임 완주율 | Threshold (빨강/앰버/초록) |
| ⏱ Time Attack Avg | 타임어택 평균 클리어 타임 | 에메랄드 |
| 📖 Story Avg | 스토리 평균 클리어 타임 | 앰버 |

**Row 2~4 — 분석 (4패널)**

| 패널 | 설명 |
|------|------|
| Events Over Time | 시간대별 이벤트 추이 (인디고 타임시리즈) |
| Avg Clear Time by Grid | 그리드 크기별 모드별 평균 클리어 타임 (바 차트) |
| Stage Funnel | 스테이지별 이탈 분석 |
| Play Time Distribution KST | 시간대별 플레이 히트맵 |

### 색상 디자인
- 일반 차트: `#818CF8` (인디고)
- Time Attack: `#34D399` (에메랄드)
- Story: `#FBBF24` (앰버)
- KPI 숫자: `#E2E8F0` (쿨 화이트)

---

## 📊 이벤트 스키마

### 공통 필드
```json
{
  "event_id": "uuid-v4",
  "event_type": "stage_start | tap_rotate | stage_clear | ...",
  "user_key": "anonymous-user-key",
  "session_id": "uuid-v4",
  "timestamp": "2026-02-26T09:11:00.000Z",
  "app_version": "1.0.0"
}
```

### 주요 이벤트

| 이벤트 | 설명 | 핵심 필드 |
|--------|------|-----------|
| `session_start` | 게임 진입 | device_info, platform |
| `stage_start` | 스테이지 시작 | stage_id, mode, grid_size |
| `tap_rotate` | 회로 조각 회전 | position, piece_type, rotation |
| `bonus_collected` | 보너스 포인트 경유 | bonus_position, route_length |
| `stage_clear` | 스테이지 클리어 | total_taps, time_seconds, score |
| `story_stage_clear` | 스토리 모드 클리어 | grid_size, clear_time_ms |
| `stage_fail` | 스테이지 실패/포기 | reason, completion_percentage |
| `time_attack_end` | 타임어택 종료 | stages_cleared, total_score |
| `leaderboard_view` | 랭킹 조회 | board_type, user_rank |

---

## 🗺️ 로드맵 & 진행 상황

### Phase 1 — MVP

| 주차 | 작업 | 상태 |
|------|------|------|
| Week 1 | 프론트엔드 전체 구현 (게임 로직, 스토리/타임어택 모드, 네온 연출, 이벤트 로깅) | ✅ 완료 |
| Week 2 | 백엔드 API (FastAPI) + 데이터 파이프라인 (Kafka → ClickHouse) + Grafana 대시보드 v8 | ✅ 완료 |
| Week 3 | 시뮬레이션 데이터 생성, 대시보드 고도화, 앱인토스 등록 준비 | 🔄 진행 중 |
| Week 4 | 앱인토스 콘솔 등록 + WebView SDK 연동 + 검수 제출 | ⬜ 예정 |

### Phase 1.5 — 완료된 추가 구현 ✅

- [x] 타임어택 시간 옵션 (60/120/180초)
- [x] 게임 내 랭킹 화면
- [x] 스토리 모드 이벤트 연동 (story_stage_clear)
- [x] 모든 기획서 이벤트 구현 (tap_rotate, session_start, bonus_collected 등)
- [x] n8n → Google Sheets 일일 자동 백업
- [x] Grafana 9패널 대시보드 (모드별 색상 분리)
- [x] 민감정보 제거 + .env.example 분리

### Phase 2 — 확장 (출시 후)
- [ ] AI 스테이지 생성 + 유저 평가 시스템
- [ ] 가챠/뽑기 시스템 (유저 리텐션 데이터 기반 도입 판단)
- [ ] 스킨 시스템 (스테이지 클리어 기반 해금)
- [ ] 앱인토스 SDK 심화 연동 (토스 로그인, 공유 리워드)

---

## 📂 프로젝트 구조

```
circuit-connect/
├── README.md
├── circuit-connect/           # 프론트엔드 (Next.js)
│   ├── src/
│   │   ├── app/               # Next.js App Router
│   │   ├── components/        # 게임 UI 컴포넌트
│   │   ├── lib/               # 게임 로직, 이벤트 모듈
│   │   └── types/             # TypeScript 타입 정의
│   └── package.json
├── backend/                   # FastAPI 백엔드
│   ├── app/
│   │   ├── main.py            # FastAPI 엔트리포인트
│   │   ├── kafka_producer.py  # Kafka 이벤트 전송
│   │   └── api/               # 엔드포인트
│   ├── docker-compose.yml     # 백엔드 컨테이너
│   ├── .env.example           # 환경변수 템플릿
│   └── requirements.txt
└── grafana/                   # Grafana 대시보드
    └── circuit-connect-dashboard.json
```

---

## 🚀 실행 방법

### 프론트엔드
```bash
cd circuit-connect/circuit-connect
npm install
npm run dev
```

### 백엔드
```bash
cd circuit-connect/backend
cp .env.example .env  # 환경변수 설정
docker-compose up -d
pip install -r requirements.txt
uvicorn app.main:app --host 0.0.0.0 --port 8000
```

---

## 🔗 관련 링크

- [앱인토스 개발자센터](https://developers-apps-in-toss.toss.im/)
- [앱인토스 GitHub 예제](https://github.com/toss/apps-in-toss-examples)
- [앱인토스 개발자 커뮤니티](https://techchat-apps-in-toss.toss.im/)

---

## 👤 개발자

**이준서** — 데이터 엔지니어링 포트폴리오 프로젝트

---

